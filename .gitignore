# 依赖与构建产物
node_modules/
dist/
build/
*.tgz
.artifacts/

# 本地备份快照（含修复前的 .bak），不进仓库
_backups/
*.bak
*.bak-*

# 机密
.env
.env.*
!.env.example
.credentials*
*.key
*.pem

# 临时 / 调试产物
temp_*.json
crop_test.png
test.vtt
*.log

# 一次性补丁 / 重写脚本（改动应直接落在源码里，见 AGENTS.md）
scripts/patch_*.py
scripts/apply_*.py
scripts/rewrite_*.py
tools/
build_v*.cjs
build_*.cjs
apply_v*.cjs
sync_demo*.cjs
*_b64.txt

# 编辑器与系统
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# agent 生成的一次性 Python 补丁脚本（见 AGENTS.md）
scripts/*.py
